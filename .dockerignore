node_modules
dist
dist-ssr
.git
.gitignore
Dockerfile
docker-compose.yml
.dockerignore
npm-debug.log*
*.md
.vscode
.idea
.DS_Store
*.local
# NOTE: `.env` is intentionally included in the build context so the Vite
# production build can inline the real Firebase web config (VITE_* vars are
# public by design). `.env.example` and other per-environment variants stay
# excluded.
.env.*
